install python 
 -- sudo apt update
 -- sudo apt install python3 python3-pip -y

install Scapy
 -- pip3 install scapy

find network 
 -- ip a
    wlp1s0 = wifi
    enp3so = ethernet

Run program
 -- sudo python3 sniffer.py
 -- sudo python3 sniffer2.py -v -i wlp1so